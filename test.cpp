#include <stdio.h>

int main () {
	int soKhoi;
	int sum1 = soKhoi*5937;
	int sum2 = soKhoi*7052;
	int sum3 = soKhoi*8669;
	int sum2 = soKhoi*15925;
	printf("moi nhap vao so khoi nuoc nha ban dung");
	if ( soKhoi < 10 ) {
		printf("so tien phai tra la : %d ", sum1);
	} else if ( soKhoi < 20 ) {
		printf("so tien phai tra la : %d ", sum2);
	} else if ( soKhoi < 30 ) {
		printf("so tien phai tra la : %d ", sum3);
	} else  {
		printf("so tien phai tra la : %d ", sum4);
	
}
